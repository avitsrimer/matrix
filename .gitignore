# Secrets — NEVER commit
.env
certs/
synapse/signing.key

# Rendered configs (generated from *.template by setup.sh)
synapse/homeserver.yaml
coturn/turnserver.conf
nginx/nginx.conf
element/config.json
docker-compose.yml

# Docker volumes
postgres-data/
synapse-media/

# OS junk
.DS_Store
Thumbs.db
